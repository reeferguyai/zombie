export class UIScene extends Phaser.Scene {
    constructor() {
        super({ key: 'UIScene' });
    }

    create() {
        console.log('Creating UI...');
        
        // Create HUD elements
        // Inventory
        // Status indicators
    }

    update(time, delta) {
        // Update UI
    }
}